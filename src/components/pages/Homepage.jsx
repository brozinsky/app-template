import React from "react";

const Homepage = ({title}) => {
    return (
        <>
            <h1 className="text-3xl text-center text-primary-500 dark:text-primary-200">{title}</h1>
        </>
    );
};

export default Homepage;
