import React from "react";
import Input from "@/elements/form/Input";

const Form = () => {
    return (
        <form>
            <input type="text" />
            <label>
                <Input />
            </label>
        </form>
    );
};

export default Form;
