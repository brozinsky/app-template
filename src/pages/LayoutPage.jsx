import WithWrapper from "@/components/HOC/WithWrapper";

const LayoutPage = () => {
    return (
        <>
            <div className="w-full h-full grid grid-cols-2">Layout</div>
        </>
    );
};

export default WithWrapper(LayoutPage);
